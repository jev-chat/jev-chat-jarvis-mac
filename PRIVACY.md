# 隐私与数据说明

jev-jarvis 的核心原则是**纯只读**：不注入、不 hook、不解密微信数据库、不自动发送任何消息。
这一页说清另一件事：**哪些数据会离开你的电脑、发到哪里**。两层模型都可以完全本地运行——
判断层默认本地，生成层可选本地 Ollama，此时整个应用不出网。

## 不会离开你电脑的部分

- **屏幕抓图与 OCR**：只读你自己屏幕上、你自己微信窗口的内容；抓图仅在内存与临时目录
  中处理，不保存到磁盘、不随日志记录。
- **日志**：不含消息正文、会话背景与候选回复文字；模型服务的错误正文也不回显到日志。
- **判断层（默认）**：本地模型 decider-2b，完全离线。
- **密钥**：保存在 `~/.config/jev-jarvis/env`（权限 600），不上传。

## 本地会话数据

历史记录默认关闭。开启后，每个可确认聊天仅保留实际识别的最近 100 条已发送消息（含本人和其他成员），不导入或补齐完整微信聊天。多行背景独立保存，关闭历史仍可使用背景与当前画面。

历史与背景以明文保存在本机的 `~/Library/Application Support/jev-jarvis/conversations.json`，源码运行与打包 `.app` 使用同一位置，文件权限为 `600`。该目录在 Git 仓库之外，聊天内容不会进入代码提交。按聊天标题隔离，首版要求单账号、唯一标题；标题不确定时不读写，改名建立新会话。

推理默认使用最新 20 条，可调 1—100，包含当前消息；背景不占条数。请求可能因长度预算进一步裁剪，本地原文不变。关闭开关保留数据并停止记录和历史注入；在「会话记录与背景 → 管理记录」清空所选会话／全部记录会保留背景；清空背景编辑框后保存可单独删除背景。删除应用不会自动删除会话数据文件。

**本地保存不等于推理不外发。** 推理使用用户配置的生成和判断服务，包含配置的中转；服务地址可在对应模型设置页配置。若在线判断失败，现有回退机制可改用本地判断。模型服务配置变更需重启生效。应用不增加历史同步、项目后台存档或遥测上传，也不保证模型服务或中转服务零留存。

## 生成层：聊天文字发到哪里

悬浮窗生成候选时，会把当前待回复消息、选取的会话上下文和人工背景发给配置的生成模型。使用云端 Jev 时，同一份内容也用于判断和排序；排序还会发送候选。发给谁由你的配置决定，
按隐私程度从高到低有三档：

### 方式一（隐私最优）：本地模型，完全不出网

生成层配本地 Ollama：`OPENAI_BASE_URL="http://localhost:11434/v1"`、密钥 `ollama`、模型如
`qwen2.5:7b`（详见 README「配置」一节）。此时聊天文字只在本机内传输，**不出网**；模型文件在
Ollama 自己的目录（`~/.ollama`），由 Ollama 自行管理，本项目不代为下载。

### 方式二（推荐）：配置你自己的 API key

按 README「配置」一节填 `OPENAI_API_KEY` / `ANTHROPIC_API_KEY`（DeepSeek、智谱等均可）。
此时聊天文字**直接从你的电脑发送给你自己选择的服务商**，不经过本项目作者的任何服务器。

### 方式三（零配置）：内置免费通道

不配任何 key 时，应用使用打包内置的共享 key，走**作者自建并运营的模型中转服务**生成候选：

- **传输内容**：当前消息、本次选取的对话历史和人工背景（生成候选所需）；
- **传输路径**：你的电脑 → 作者的中转服务器 → 中转所代理的模型服务商（当前为智谱 glm-4-flash）；
- **承诺**：聊天内容仅用于生成回复并即时返回，**不用于其他任何用途、不出售、不与任何第三方共享**，
  也不与你的微信账号或设备身份关联；
- **两点如实提醒**：该链路会经过上游模型服务商，其数据处理遵循其自身政策；中转端点目前为
  HTTP 明文传输，理论上可被网络路径上的第三方截获——**对隐私要求高的场景请用方式一或方式二**。

## 使用与免责

- 候选回复由模型自动生成，仅供参看；是否采纳、是否发送完全由你决定，本项目不对生成内容的
  准确性与得体性负责。
- 使用内置免费通道即表示你已理解并接受上述数据流向；不接受请改用方式一或方式二。
- 免费通道额度有限，可能随时调整或停止，不构成可用性承诺。
